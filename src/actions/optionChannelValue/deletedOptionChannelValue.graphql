mutation deletedOptionChannelValue($id : ID!){
  deletedOptionChannelValue(id: $id) {
    id
    name
    value
    status
    userId
    isProperties
    isCustomValue
    optionChannelTypeId
    createdAt
    updatedAt
    deletedAt
  }
}
