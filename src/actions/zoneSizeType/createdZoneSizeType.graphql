mutation createdZoneSizeType($zoneSizeType: ZoneSizeTypeInputTypeWithoutId!) {
  createdZoneSizeType(zoneSizeType: $zoneSizeType) {
    id
    name
    width
    height
    status
    userId
    createdAt
    updatedAt
  }
}
