mutation updatedZoneSizeType($zoneSizeType: ZoneSizeTypeInputType!) {
  updatedZoneSizeType(zoneSizeType: $zoneSizeType) {
    id
    name
    width
    height
    status
    userId
    createdAt
    updatedAt
  }
}
