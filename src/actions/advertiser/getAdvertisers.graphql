query getAdvertisers($where: SequelizeJSON) {
  advertisers(where: $where) {
    id
    email
    name
    contact
    isEmailReport
    isEmailStatus
    reportInterval
    description
    status
    createdAt
    updatedAt
  }
}
