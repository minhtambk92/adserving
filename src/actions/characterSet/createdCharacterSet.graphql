mutation createdCharacterSet($characterSet: CharacterSetInputTypeWithoutId!) {
  createdCharacterSet(characterSet: $characterSet) {
    id
    name
    value
    status
    userId
    createdAt
    updatedAt
  }
}
