mutation deletedOptionChannelType($id: ID!){
  deletedOptionChannelType(id: $id) {
    id
    name
    isInputLink
    isSelectOption
    isVariable
    status
    userId
    createdAt
    updatedAt
    deletedAt
  }
}
