mutation deletedOptionChannelType($id: ID!){
  deletedOptionChannelType(id: $id) {
    id
    name
    isInputLink
    isSelectOption
    isVariable
    isMultiSelect
    status
    userId
    createdAt
    updatedAt
    deletedAt
  }
}
