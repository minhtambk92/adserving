mutation createActivity($activity: ActivityInputTypeWithoutId!) {
  createdActivity(activity: $activity) {
    id
    action
    subject
    subjectId
    other
    userId
    createdAt
    updatedAt
  }
}
